node_modules
dist
build
.output
.netlify
.vinxi
pnpm-lock.yaml
package-lock.json
bun.lock
routeTree.gen.ts

# Verbatim source documents. docs/spec.md is the requirements document and
# docs/lovable-teardown.md is the brief this repo was reworked against; both
# are records of what someone else wrote, not our source. Prettier rewrites
# trailing whitespace, which is a meaningful line break in markdown, so
# formatting them edits the evidence.
docs/spec.md
docs/lovable-teardown.md

# Walkthrough containing code quoted verbatim from source files. Prettier
# reformats fenced code blocks, which silently rewrote a quoted line into
# something that no longer matched src/content/site.ts — exactly the line a
# beginner is told to go and find.
docs/GETTING-STARTED.md
